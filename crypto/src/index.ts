import crypto from 'crypto';

export default class SpacedCrypto {
    constructor() {}

    /**
     * Generate a key encryption key from a password.
     */
    static getKeyEncryptionKey(password: string) {

    }
}